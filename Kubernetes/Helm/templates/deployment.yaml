#---------------------------
# DribbleData Project
# Created by Matvey Guralskiy
#---------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-deployment
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Release.Name }}
    env: {{ .Values.env }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      containers:
        - name: main-container
          image: matveyguralskiy/dribbledata_main:V0.3
          ports:
            - containerPort: 80
        - name: users-container
          image: matveyguralskiy/dribbledata_users:V0.3
          ports:
            - containerPort: 80
        - name: training-container
          image: matveyguralskiy/dribbledata_training:V0.3
          ports:
            - containerPort: 80
        - name: players-container
          image: matveyguralskiy/dribbledata_players:V0.3
          ports:
            - containerPort: 80
